name: Production Deploy

on:
  push:
    branches:
      - main

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    env:
      JWT_SECRET: test-secret-for-ci
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
          cache: gradle

      - name: Grant Gradle execute permission
        run: chmod +x gradlew

      - name: Run tests
        run: ./gradlew test

  build-and-push:
    runs-on: ubuntu-latest
    needs: test
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push API image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          target: api
          push: true
          tags: |
            ${{ secrets.DOCKERHUB_USERNAME }}/tmk-api:${{ github.sha }}
            ${{ secrets.DOCKERHUB_USERNAME }}/tmk-api:prod-latest

      - name: Build and push batch image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          target: batch
          push: true
          tags: |
            ${{ secrets.DOCKERHUB_USERNAME }}/tmk-batch:${{ github.sha }}
            ${{ secrets.DOCKERHUB_USERNAME }}/tmk-batch:prod-latest

  deploy:
    runs-on: ubuntu-latest
    needs: build-and-push
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Copy deployment files to server
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.PROD_HOST }}
          username: ${{ secrets.PROD_USER }}
          key: ${{ secrets.PROD_SSH_KEY }}
          source: "docker-compose.prod.yml,docker/postgres/init.sql"
          target: ${{ secrets.PROD_DEPLOY_PATH }}
          strip_components: 0

      - name: Deploy to production server
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.PROD_HOST }}
          username: ${{ secrets.PROD_USER }}
          key: ${{ secrets.PROD_SSH_KEY }}
          script: |
            set -eu

            DEPLOY_PATH="${{ secrets.PROD_DEPLOY_PATH }}"
            mkdir -p "$DEPLOY_PATH/docker/postgres"
            cd "$DEPLOY_PATH"

            cat > .env.prod <<EOF
            POSTGRES_DB=${{ secrets.PROD_POSTGRES_DB }}
            POSTGRES_USER=${{ secrets.PROD_POSTGRES_USER }}
            POSTGRES_PASSWORD=${{ secrets.PROD_POSTGRES_PASSWORD }}
            POSTGRES_PORT=${{ secrets.PROD_POSTGRES_PORT }}
            REDIS_PORT=${{ secrets.PROD_REDIS_PORT }}
            REDIS_PASSWORD=${{ secrets.PROD_REDIS_PASSWORD }}
            API_PORT=${{ secrets.PROD_API_PORT }}
            DOCUMENT_STORAGE_DIR=${{ secrets.PROD_DOCUMENT_STORAGE_DIR }}
            DOCKERHUB_NAMESPACE=${{ secrets.PROD_DOCKERHUB_NAMESPACE }}
            JWT_SECRET=${{ secrets.PROD_JWT_SECRET }}
            OPENAI_API_KEY=${{ secrets.PROD_OPENAI_API_KEY }}
            OPENAI_EMBEDDING_MODEL=${{ secrets.PROD_OPENAI_EMBEDDING_MODEL }}
            OPENAI_CHAT_MODEL=${{ secrets.PROD_OPENAI_CHAT_MODEL }}
            MAIL_HOST=${{ secrets.PROD_MAIL_HOST }}
            MAIL_PORT=${{ secrets.PROD_MAIL_PORT }}
            MAIL_USERNAME=${{ secrets.PROD_MAIL_USERNAME }}
            MAIL_PASSWORD=${{ secrets.PROD_MAIL_PASSWORD }}
            SWAGGER_ENABLED=${{ secrets.PROD_SWAGGER_ENABLED }}
            BATCH_JOB_ENABLED=${{ secrets.PROD_BATCH_JOB_ENABLED }}
            BATCH_INITIALIZE_SCHEMA=${{ secrets.PROD_BATCH_INITIALIZE_SCHEMA }}
            IMAGE_TAG=${{ github.sha }}
            EOF

            docker login -u "${{ secrets.DOCKERHUB_USERNAME }}" -p "${{ secrets.DOCKERHUB_TOKEN }}"
            docker compose --env-file .env.prod -f docker-compose.prod.yml pull
            docker compose --env-file .env.prod -f docker-compose.prod.yml up -d
